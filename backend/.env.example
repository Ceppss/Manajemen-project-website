# Copy this file to .env and fill in real values. .env is gitignored.

# SMTP for notification emails. For Gmail: create an App Password at
# https://myaccount.google.com/apppasswords (needs 2FA enabled on the account)
# and use it as SMTP_PASS - your normal Gmail password will not work.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=rafipramana21@gmail.com
SMTP_PASS=
SMTP_FROM=MediTrans <rafipramana21@gmail.com>

JWT_SECRET=meditrans-dev-secret
